# Contributing

Use .NET 9 and nullable C#. Keep control and media planes separate, add deterministic tests for routing and loop prevention, and document architectural choices as ADRs. Run `dotnet build OpenCallMesh.sln` and the test harnesses before submitting changes.
